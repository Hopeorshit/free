 <map
      id="myMap"
      style="width: 100%; height: 600px;"
      latitude="{{latitude}}"
      longitude="{{longitude}}"
      markers="{{markers}}"
      covers="{{covers}}"
      show-location
    ></map>
    <view class="address">
    <text>地址：集美基金迷迷糊糊都会</text>
    </view>
  
